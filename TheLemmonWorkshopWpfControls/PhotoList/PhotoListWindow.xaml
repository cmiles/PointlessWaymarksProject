<Window x:Class="TheLemmonWorkshopWpfControls.PhotoList.PhotoListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TheLemmonWorkshopWpfControls.PhotoList"
        xmlns:controlStatus="clr-namespace:TheLemmonWorkshopWpfControls.ControlStatus"
        mc:Ignorable="d"
        Title="PhotoListWindow" Height="800" Width="800" d:DataContext="{d:DesignInstance local:PhotoListWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:PhotoListControl DataContext="{Binding ListContext}" />
        <StackPanel Grid.Row="1" Margin="4" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Command="{Binding EditSelectedContentCommand}">Edit</Button>
            <Button Command="{Binding GenerateSelectedHtmlCommand}">Generate Html</Button>
        </StackPanel>
    </Grid>
</Window>