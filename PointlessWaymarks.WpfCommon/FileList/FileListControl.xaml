<UserControl x:Class="PointlessWaymarks.WpfCommon.FileList.FileListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointlessWaymarks.WpfCommon.FileList"
             xmlns:behaviors="clr-namespace:PointlessWaymarks.WpfCommon.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:FileListViewModel}">
    <Grid>
        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <GroupBox Header="Add">
                <StackPanel>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,8">
                        <WrapPanel.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Width" Value="180" />
                            </Style>
                        </WrapPanel.Resources>
                        <Button Command="{Binding AddFilesToTagFromDirectoryCommand}">Directory</Button>
                        <Button Command="{Binding AddFilesToTagFromDirectoryAndSubdirectoriesCommand }">Directory with Subdirectories</Button>
                        <Button Command="{Binding AddFilesToTagCommand}">Files</Button>
                    </WrapPanel>
                </StackPanel>
            </GroupBox>
            <GroupBox Grid.Row="1" Header="Files To Tag">
                <DataGrid Margin="2" ItemsSource="{Binding Files}" CanUserAddRows="False"
                                  CanUserReorderColumns="true"
                                  CanUserDeleteRows="true" CanUserResizeColumns="true" CanUserResizeRows="False"
                                  CanUserSortColumns="true" AutoGenerateColumns="False" IsReadOnly="True" SelectedItem="{Binding SelectedFile}"  behaviors:MultiSelectorBehaviours.SynchronizedSelectedItems="{Binding SelectedFiles}">
                    <DataGrid.ContextMenu>
                        <ContextMenu ItemsSource="{Binding ContextMenuItems}">
                            <ContextMenu.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Header" Value="{Binding ItemName}" />
                                    <Setter Property="Command" Value="{Binding ItemCommand}" />
                                </Style>
                            </ContextMenu.ItemContainerStyle>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*" Header="File" Binding="{Binding FullName}" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
