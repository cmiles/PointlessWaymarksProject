<Window x:Class="PointlessWaymarksCmsWpfControls.ImageList.ImageListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PointlessWaymarksCmsWpfControls.ImageList"
        xmlns:status="clr-namespace:PointlessWaymarksCmsWpfControls.Status"
        mc:Ignorable="d"
        Title="Image List" Height="450" Width="800" d:DataContext="{d:DesignInstance local:ImageListWindow}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../WpfControlsResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="240" />
        </Grid.ColumnDefinitions>
        <local:ImageListControl DataContext="{Binding ListContext}" Margin="4,5,4,6" />
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <Button Style="{StaticResource LargerButton}" Command="{Binding EditSelectedContentCommand}">Edit</Button>
            <Button Style="{StaticResource LargerButton}" Command="{Binding GenerateSelectedHtmlCommand}">Generate Html</Button>
            <Button Style="{StaticResource LargerButton}" Command="{Binding ImageCodesToClipboardForSelectedCommand}">Photo Code to Clipboard</Button>
            <Button Style="{StaticResource LargerButton}" Command="{Binding OpenUrlForSelectedCommand}">Open Selected in Browser</Button>
        </StackPanel>
        <status:StatusControl Grid.Column="0" Grid.ColumnSpan="2" d:IsHidden="true"
                              DataContext="{Binding StatusContext}" />
    </Grid>
</Window>